FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install --production

COPY . .

# Create uploads directory (shared volume with server)
RUN mkdir -p uploads

CMD ["node", "worker.js"]
